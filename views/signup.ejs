<link rel="stylesheet" href="/stylesheets/account/form.css" />
<script src="/javascripts/account/form.js"></script>
<script src="/javascripts/account/signup.js"></script>
<section>
  <div class="mb-4">
    <h1>
      <span>팥붕슈붕에 가입하기</span>
    </h1>
  </div>
  <form class="needs-validation" novalidate>
    <div class="mb-5">
      <label for="email" class="form-label">이메일</label>
      <input
        type="email"
        class="form-control"
        id="email"
        required
        pattern="^[^\s@]+@[^\s@]+\.[^\s@]+$"
      />
      <div class="invalid-feedback">이메일 주소를 입력해주세요</div>
    </div>
    <div class="mb-5">
      <label for="password" class="form-label">비밀번호</label>
      <input
        type="password"
        class="form-control"
        id="password"
        aria-describedby="passwordHelpBlock"
        required
        pattern=".{8,}"
      />
      <div id="passwordHelpBlock" class="form-text">
        비밀번호는 8자 이상이어야 해요
      </div>
      <div class="invalid-feedback">8자 이상의 비밀번호를 입력해주세요</div>
    </div>
    <div class="mb-5">
      <label for="password-re" class="form-label">비밀번호 확인</label>
      <input
        type="password"
        class="form-control"
        id="password-re"
        aria-describedby="passwordHelpBlock"
        required
        pattern=".{8,}"
      />
      <div class="invalid-feedback">8자 이상의 비밀번호를 입력해주세요</div>
    </div>
    <div class="footer">
      <button id="submit" type="submit" class="btn btn-primary disabled">
        가입하기
      </button>
      <span>계정이 있으신가요? <a href="/account/signin">로그인하기</a></span>
    </div>
  </form>
</section>
